@page
@model CaveroClubhuis.Pages.AdminModel
@{
    ViewData["Title"] = "Admin";
}

<head>
    <link rel="stylesheet" href="~/css/Admin.css" asp-append-version="true" />
</head>
<body>
    <div class="main-body">
        <div class="admin-bar">
            <!-- Buttons for the admin-->
            <a href="/MakeEvent">
                <button id="makeButton" class="admin-button">
                    Event aanmaken
                </button>
            </a>
            <a href="/AdminChange">
                <button id="changeButton" class="admin-button">
                    Event aanpassen
                </button>
            </a>
            <a href="/AdminDelete">
                <button id="removeButton" class="admin-button">
                    Event verwijderen
                </button>
            </a>
        </div>

        <div id="change" class="make-event" style="display:none;">
            change
            <!-- Add the change event inputs here-->
        </div>

        <div id="remove" class="make-event" style="display:none;">
            <!-- Add the remove event inputs here-->
        </div>
    </div>
</body>


<script>
    function toggleShow(id, class1) {
        var allElements = document.querySelectorAll(class1);
        allElements.forEach(function (element) {
            if(element.style.display === "flex")
            {
                element.style.display = "none";
            }
            else if (element.id === id)
            {
                element.style.display = "flex";
            }
        });
    }
    function toggleColor(id, class1) {
        var allElements = document.querySelectorAll(class1);
        allElements.forEach(function (element) {
            if (element.id === id) {
                element.style.backgroundColor = "#903689";
                element.style.color = "white";
            }
            else
            {
                element.style.background = "red";
            }
        });
    }
</script>